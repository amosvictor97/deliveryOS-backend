const express = require("express");
const router = express.Router()

const trackingStepController = require('../controllers/trackingStepController')


/* router.post('/',trackingStepController)

router.put('/(:id)',trackingStepController) */


module.exports = router;